No crear todos los usuarios de golpe. Crearlos de forma gradual.
Conseguir el 80% y 20% a lo largo del tiempo.
Solución compleja: Si un usuario quiere buscar una palabra, que se haga a la vez en varios libros
y en cada libro hay un hilo en cada una de las partes. Varios libros a la vez, varios hilos en cada libro

El usuario realiza la petición al sistema de búsqueda. Si el usuario no tiene el saldo suficiente, el sistema de búsqueda
se comunicará con el sistema de pago y continuará con el proceso, donde el usuario será notificado.
Podríamos tener una cola de peticiones entre el usuario y el sistema de búsqueda y otra cola de peticiones entre el 
sistema de búsqueda y el sistema de pago.

El cliente se bloquea hasta que reciba los resultados y será el cliente quien muestre los resultados

Mostrar por pantalla información únicamente de lo que esta pasando. Se recomienda el uso de colores.
El cliente deberá crear un log y guardar toda la información que recibe en lugar de mostrar por pantalla.

Ir de menos a más.
